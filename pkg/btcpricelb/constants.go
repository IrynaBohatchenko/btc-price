package btcpricelb

const EmailForm = "email"
const ErrorEmailExists = "email already exists"

const CoingeckoRatePath = "https://api.coingecko.com/api/v3/simple/price?ids=bitcoin&vs_currencies=uah" // could be moved to config
const StoragePath = "./emails.txt"                                                                      // could be moved to config
